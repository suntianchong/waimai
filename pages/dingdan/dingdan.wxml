<!--pages/dingdan/dingdan.wxml-->
<view class="order">订单信息</view>
<view class="orderContent" wx:for="{{orderList}}" >
<view>{{item.name}}</view>
<view>{{item.price}}</view>
<view>{{item.count}}</view>
</view>